# @stout/hello-npm

A minimal, dependency-free npm package used as a curated build-from-source sample
for Stout end-to-end validation. No dependencies, no install/lifecycle scripts, no
network access — so a build is deterministic and never depends on an external fetch.
